# =============================================================================
# GitHub Actions · Secrets（敏感）
# =============================================================================
#
# 用途：CD SSH 认证与可选 token（L2 Secrets）。切勿 commit 真实值。
# 本地路径：~/.config/xiaolinstar/ai-todo/github/<environment>/secrets.env
# 同步命令：sync.sh env sync-github --project ai-todo --environment <environment>
#
# 说明：
#   · 所有 GitHub Environment 使用**相同键名**；每个 Environment 填各自服务器的凭证
#   · SSH 认证二选一 [必填其一]；默认使用 DEPLOY_PASSWORD（SSH 登录密码）
#
# --- SSH 认证（二选一 [必填其一]）---
#
# 默认：SSH 登录密码（推荐）
DEPLOY_PASSWORD=

# 备选：SSH 私钥文件路径（取消注释并填写；sync 读取后推送为 GitHub Secret DEPLOY_SSH_KEY）
# CD 若同时存在 DEPLOY_SSH_KEY 与 DEPLOY_PASSWORD，优先使用密钥
# DEPLOY_SSH_KEY_FILE=$HOME/.ssh/ai_todo_deploy
#
# 备选 inline（一般不推荐，私钥不宜长期留在明文文件）：
# DEPLOY_SSH_KEY=
# DEPLOY_SSH_KEY_B64=

# --- GHCR [可选] 拉取私有镜像时填写 ---
# GHCR_DEPLOY_TOKEN=

# --- 冒烟测试 [可选] 需登录态的 post-deploy 检查 ---
# CD_SMOKE_PAT=
