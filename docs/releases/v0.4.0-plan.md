# ai-todo v0.4.0 版本规划 — npm 全局安装与配置优先 UX

## 目标

1. **`@xiaolinstar/ai-todo-cli` 发布到 npm**，支持 `npm install -g @xiaolinstar/ai-todo-cli`（命令仍为 `ai-todo`）。
2. **单包打包**：esbuild 将 `@ai-todo/api-client`、`@ai-todo/shared` 打入 CLI 产物，不单独发布 workspace 包。
3. **配置优先**：用户编辑 `~/.ai-todo/settings.json` 即可使用；`login` / `token` / `logout` 从公开 help 移除（实现保留兼容）。
4. **小程序 onboarding**：创建令牌后复制 JSON 配置片段，而非 `ai-todo login` 命令。

## 组件版本

| 组件       | L1 版本         | 说明                                |
| ---------- | --------------- | ----------------------------------- |
| CLI        | `0.4.0`         | npm 发布、esbuild bundle、help/文档 |
| 微信小程序 | `0.4.0`         | 复制 settings JSON                  |
| API        | 不变（`0.2.0`） | 无契约变更                          |

Git `release_tag` 建议：`v0.4.0`

## npm 发布

```bash
cd apps/cli
pnpm build
npm publish --access public --registry https://registry.npmjs.org
```

前置：npm 账号 `xiaolinstar` 已登录；scope `@xiaolinstar` 可用（无需单独创建组织）。

> 无 scope 名 `ai-todo-cli` 已被占用；`@ai-todo` 组织未创建，故使用 `@xiaolinstar/ai-todo-cli`。安装后全局命令仍为 **`ai-todo`**。

发布物：

- `dist/index.js`（bundle + sourcemap）
- `settings.example.json`
- `README.md`

## 用户路径

```
npm install -g @xiaolinstar/ai-todo-cli
小程序创建 PAT → 复制 JSON → ~/.ai-todo/settings.json
ai-todo today
```

## 验收

- [ ] `pnpm --filter @xiaolinstar/ai-todo-cli build` 产出可执行 bundle
- [ ] `ai-todo help` 无 login/token 段落
- [ ] 未配置时错误提示指向 settings.json
- [ ] 小程序创建令牌复制 JSON
- [ ] `pnpm test:api` 通过
- [ ] `pnpm check:wechat` 通过
