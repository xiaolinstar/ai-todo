# v0.8.13 小程序真机验收记录

日期：2026-07-13

## 范围

- 验收对象：`@ai-todo/miniapp@0.8.13`
- 后端基线：production-k8s `releaseTag=v0.8.13`，`apiVersion=0.5.1`
- 重点：真机预览/体验版前的版本可见性、构建健康、登录与 API 环境核对

## 已完成

- `pnpm check:wechat` 通过：TypeScript、ESLint、Prettier、小程序编译检查、四件套结构检查均为 success。
- `pnpm miniapp:preview` 已完成本地编译、代码分析与打包。
- 小程序版本号已同步为 `0.8.13`：
  - `apps/miniapp/package.json`
  - `apps/miniapp/project.config.json` 的 `versionName`
  - `apps/miniapp/miniprogram/lib/version.ts` 的 `MINIAPP_VERSION`
- `apps/miniapp/CLAUDE.md` 的当前版本提示已同步到 `0.8.13`。

## 阻断项

`pnpm miniapp:preview` 上传二维码阶段被微信官方接口拒绝：

```text
invalid ip: 183.212.181.133
```

原因是当前出口 IP 未加入「小程序代码上传密钥」IP 白名单。代码构建已通过，阻断点在微信平台上传权限。

## 待真机确认

- 「我的」→「关于」显示小程序版本 `0.8.13`。
- develop 手机预览 / 真机调试连接 `https://staging.xingxiaolin.cn`。
- trial 体验版连接 `https://staging.xingxiaolin.cn`。
- release 正式版连接 `https://xingxiaolin.cn`。
- 微信登录后提醒列表、日历、通讯录三个 Tab 首屏加载无登录态串桶。
- 新建提醒、编辑提醒、左滑操作、标签展示与筛选流程可完成。
- 通知设置页可进入，未授权/已授权状态文案可读。

## 下一步

将当前出口 IP `183.212.181.133` 加入微信公众平台上传密钥 IP 白名单，或在微信开发者工具中直接上传 `0.8.13` 体验版，然后按上方清单扫码验收。
