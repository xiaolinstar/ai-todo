#!/usr/bin/env sh
# Commit-msg hook: enforce conventional commits via commitlint
# Spec: https://www.conventionalcommits.org/

# Source husky.sh for v8 compat; v9 is no-op
. "$(dirname -- "$0")/_/husky.sh" 2>/dev/null || true

npx --no -- commitlint --edit "$1"
